<h1><%= t('editing', name: t('product.title', count: 1)) %></h1>

<%= render 'form' %>

<%= link_to t('show'), list_products_path(@list, @product), class: 'btn btn-info', role: :button %>
<%= link_to t('index'), list_products_path(@list), class: 'btn btn-outline-secondary', role: :button %>
